# ============================================
# Frontend — Environnement dev local
# ============================================
# Pointe vers le backend Django local (port 8000)
# Le proxy Vite redirige /api -> http://127.0.0.1:8000/api

VITE_API_URL=/api
