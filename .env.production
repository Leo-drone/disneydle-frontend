# ============================================
# Frontend — Environnement production (Docker/Nginx)
# ============================================
# En prod, Nginx fait le reverse proxy /api -> backend:8000

VITE_API_URL=/api
